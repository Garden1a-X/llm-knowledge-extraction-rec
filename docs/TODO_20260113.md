# TODO - 2026-01-13 - 消融实验与结果分析

## 🌅 Session Goals

基于昨天的进展，今天的核心任务是：
1. **查看tune1结果，决定是否继续调参**
2. **运行消融实验（核心）**
3. **整理结果，准备论文材料**

---

## 📋 昨天完成了什么

### ✅ Phase 5/6完成
- User兴趣提取（6,040用户）
- RecBole格式转换

### ✅ Phase 7完成
- 模型实现（异构图、双编码器、损失函数）
- Baseline训练：**Ours-Full NDCG@10 = 0.1549**
- 超越LightGCN **22.3%**，超越BPR **27.1%** ⭐

### 🏃 第一轮调参（3/4完成）
- tune2: 0.1549（持平）
- tune3: 0.1241（下降20%，失败）
- tune4: 0.1400（下降10%，失败）
- tune1: 🏃 运行中（最有希望）

---

## 🎯 Task 1: 查看tune1结果并决策（Priority 1）

**时间**: 8:00-9:00 AM

### Step 1.1: 检查tune1状态

```bash
# 1. 检查进程
ps aux | grep train_model_fast

# 2. 查看日志
tail -100 log/tune_parallel_tune1_*.log

# 3. 查看结果（如果完成）
# 结果应该在用户本地机器的outputs/ours/目录下
```

### Step 1.2: 分析结果

**如果tune1成功（NDCG@10 > 0.1549）:**
- ✅ 说明loss weight调整有效
- 📝 记录最佳配置
- 💡 建议：可以考虑在这个方向继续fine-tune
- ⏭️ 但优先级低于消融实验

**如果tune1失败（NDCG@10 ≤ 0.1549）:**
- ❌ 第一轮调参全军覆没
- 📝 分析失败原因（过拟合、收敛过快等）
- 💡 建议：不再继续调参，专注消融实验
- ⏭️ 0.1549已经足够好（+27% vs BPR）

### Step 1.3: 更新文档

```bash
# 更新实验结果到EXPERIMENT_TRACKING.md
# 记录tune1的最终结果
```

**预计时间**: 1小时
**依赖**: tune1实验完成
**输出**: 决策是否继续调参

---

## 🎯 Task 2: 运行消融实验（Priority 1 - 核心）

**时间**: 9:00-14:00

### 为什么消融实验最重要？

消融实验是论文的**核心**，用于证明：
1. 多视图对比学习是否有效（w/o contrast）
2. Mask机制是否对抗幻觉（w/o mask）
3. CF视图的贡献（KG-only）
4. KG视图的贡献（CF-only）

**这比超参数调优更重要！**

### Step 2.1: 准备消融实验配置

检查已有配置：
```bash
ls -la configs/ours_*.yaml
```

应该有：
- ✅ configs/ours_full.yaml（baseline，已运行）
- ✅ configs/ours_wo_contrast.yaml
- ✅ configs/ours_wo_mask.yaml
- ✅ configs/ours_kg_only.yaml
- ⚠️ configs/ours_cf_only.yaml（可能需要创建）

### Step 2.2: 并行运行4个消融实验

```bash
# 使用GPU 0,5,6,7并行运行
CUDA_VISIBLE_DEVICES=0 python scripts/train_model_fast.py \
    --config configs/ours_wo_contrast.yaml \
    > log/ablation_wo_contrast_$(date +%Y%m%d_%H%M%S).log 2>&1 &

CUDA_VISIBLE_DEVICES=5 python scripts/train_model_fast.py \
    --config configs/ours_wo_mask.yaml \
    > log/ablation_wo_mask_$(date +%Y%m%d_%H%M%S).log 2>&1 &

CUDA_VISIBLE_DEVICES=6 python scripts/train_model_fast.py \
    --config configs/ours_kg_only.yaml \
    > log/ablation_kg_only_$(date +%Y%m%d_%H%M%S).log 2>&1 &

CUDA_VISIBLE_DEVICES=7 python scripts/train_model_fast.py \
    --config configs/ours_cf_only.yaml \
    > log/ablation_cf_only_$(date +%Y%m%d_%H%M%S).log 2>&1 &
```

**或者使用一键脚本**（如果创建的话）：
```bash
./run_ablation_experiments.sh
```

### Step 2.3: 监控实验进度

```bash
# 实时监控GPU
watch -n 1 nvidia-smi

# 查看日志
tail -f log/ablation_*.log

# 检查进程
ps aux | grep train_model_fast
```

### Step 2.4: 等待结果

**预计时间**: 每个实验 5-10分钟
**总时间**: ~10分钟（并行）+ 等待时间

### Step 2.5: 收集结果

结果应该在用户本地的：
```
outputs/ours/ours_wo_contrast_{timestamp}/history.json
outputs/ours/ours_wo_mask_{timestamp}/history.json
outputs/ours/ours_kg_only_{timestamp}/history.json
outputs/ours/ours_cf_only_{timestamp}/history.json
```

从每个history.json提取：
- final_test_metrics['NDCG@10']
- final_test_metrics['Recall@10']
- best_epoch
- best_ndcg

**预计时间**: 3-4小时（包括运行时间）
**依赖**: 无
**输出**: 4个消融实验结果

---

## 🎯 Task 3: 结果分析与对比（Priority 2）

**时间**: 14:00-17:00

### Step 3.1: 创建对比表格

整理所有结果到表格：

| 实验 | NDCG@10 | Recall@10 | NDCG@20 | Recall@20 | 下降幅度 | 结论 |
|------|---------|-----------|---------|-----------|----------|------|
| **Ours-Full** | 0.1549 | 0.0722 | 0.1644 | 0.1276 | - | Baseline |
| w/o Contrast | ? | ? | ? | ? | ?% | 对比学习有效性 |
| w/o Mask | ? | ? | ? | ? | ?% | Mask机制有效性 |
| KG-only | ? | ? | ? | ? | ?% | CF视图贡献 |
| CF-only | ? | ? | ? | ? | ?% | KG视图贡献 |

### Step 3.2: 分析预期结果

**预期**:
- **w/o Contrast**: 应该略有下降（3-5%）
  - 如果下降明显 → 对比学习有效 ✅
  - 如果持平/提升 → 对比学习可能不必要 ⚠️

- **w/o Mask**: 应该略有下降（2-4%）
  - 如果下降明显 → Mask对抗幻觉有效 ✅
  - 如果持平 → 幻觉问题不严重 ⚠️

- **KG-only**: 应该明显下降（10-15%）
  - 说明CF视图贡献大 ✅

- **CF-only**: 应该下降（5-10%）
  - 说明KG视图（知识图谱）有贡献 ✅

### Step 3.3: 撰写分析报告

在`docs/ABLATION_ANALYSIS.md`中记录：
1. 每个消融实验的动机
2. 实际结果
3. 分析与解释
4. 论文写作要点

### Step 3.4: 可视化（如果时间允许）

- 训练曲线对比（5条线）
- 指标柱状图对比
- Mask权重分布

**预计时间**: 3小时
**依赖**: Task 2完成
**输出**: 消融实验分析报告

---

## 🎯 Task 4: 多种子验证（Optional - Priority 3）

**时间**: 17:00-20:00（如果时间允许）

### 为什么需要多种子验证？

RecBole的baseline结果都是5-seed平均值：
- BPR: 0.1219 ± 0.0021
- LightGCN: 0.1267 ± 0.0013

你的Ours-Full目前只有1个seed的结果（0.1549）。

### Step 4.1: 准备多个seed配置

创建5个配置，只改random_seed：
```yaml
configs/ours_full_seed42.yaml  # 已运行，seed=42
configs/ours_full_seed2023.yaml  # seed=2023
configs/ours_full_seed2024.yaml  # seed=2024
configs/ours_full_seed2025.yaml  # seed=2025
configs/ours_full_seed12345.yaml # seed=12345
```

### Step 4.2: 运行5个实验

```bash
# 串行运行（每个5-10分钟，总共25-50分钟）
for seed in 2023 2024 2025 12345; do
    python scripts/train_model_fast.py \
        --config configs/ours_full_seed${seed}.yaml
done
```

### Step 4.3: 计算统计量

```python
results = [0.1549, ?, ?, ?, ?]  # 5个结果
mean = np.mean(results)
std = np.std(results)

print(f"Ours-Full: {mean:.4f} ± {std:.4f}")
```

**预计时间**: 2-3小时（如果运行）
**优先级**: 低（可以放到后面做）
**输出**: Ours-Full均值和标准差

---

## 📊 预期成果

### 今天结束时应该有：

1. ✅ tune1结果 + 调参决策
2. ✅ 4个消融实验结果
3. ✅ 完整的消融分析报告
4. ⏸️ （可选）多种子验证结果

### 论文材料准备度：

- **实验结果**: 90%完成
  - Baseline vs BPR/LightGCN ✅
  - 消融实验 ✅
  - 多种子验证 ⏸️（可选）

- **分析材料**: 70%完成
  - 对比表格 ✅
  - 消融分析 ✅
  - 可视化 ⏸️（待做）

- **论文写作**: 可以开始
  - 方法论部分（描述模型）
  - 实验设置
  - 结果与分析

---

## 🚨 潜在问题与应对

### 问题1: 消融实验结果不符合预期

**症状**: 去掉某个组件后性能反而提升

**可能原因**:
- 组件设计有问题
- 超参数不匹配
- 随机性影响

**应对**:
- 运行多个seeds验证
- 检查组件实现
- 调整该组件的超参数

### 问题2: 所有消融实验性能都相近

**症状**: w/o contrast, w/o mask等结果都接近0.1549

**可能原因**:
- 组件贡献确实不大
- Loss权重太小，组件被忽略

**应对**:
- 检查loss权重配置
- 增大alpha/beta/gamma重新运行
- 论文中诚实报告"组件贡献有限"

### 问题3: 时间不够

**症状**: 消融实验运行时间超过预期

**应对**:
- 优先保证4个消融实验完成
- 放弃多种子验证（可以后补）
- 使用混合精度训练加速

---

## 💡 改进建议（基于昨天的经验）

### 1. 不要过度追求超参数优化

**教训**: tune3/tune4失败说明盲目增加模型复杂度反而有害

**建议**:
- 0.1549已经很好（+27% vs BPR）
- 专注消融实验证明方法有效性
- 如果tune1也失败，就不要继续调参了

### 2. 保守的模型设计更稳定

**教训**: ML-1M是中等规模数据，不需要太深的模型

**当前配置已经不错**:
- 2层GNN
- 64维embedding
- dropout 0.2
- 简单的MLP融合

### 3. 消融实验比调参更重要

**原因**:
- 调参只是改进性能（锦上添花）
- 消融实验证明方法有效性（核心贡献）
- 论文审稿人更关注消融实验

### 4. 多种子验证提升可信度

**重要性**: 单一seed结果可能有运气成分

**建议**: 如果时间允许，至少跑3个seeds

---

## 📅 时间表

| 时间 | 任务 | 预计耗时 |
|------|------|----------|
| 08:00-09:00 | 查看tune1结果并决策 | 1h |
| 09:00-09:30 | 准备消融实验 | 0.5h |
| 09:30-09:45 | 启动4个并行实验 | 0.25h |
| 09:45-10:30 | 等待实验完成 | 0.75h |
| 10:30-12:00 | 收集结果，初步分析 | 1.5h |
| 12:00-13:00 | 午休 | 1h |
| 13:00-15:00 | 详细分析与撰写报告 | 2h |
| 15:00-17:00 | 可视化（可选） | 2h |
| 17:00-20:00 | 多种子验证（可选） | 3h |

---

## ✅ Success Criteria

今天结束时：
- [x] tune1结果已知，调参决策已做
- [x] 4个消融实验全部完成
- [x] 消融分析报告已撰写
- [x] 更新EXPERIMENT_TRACKING.md
- [ ] （可选）多种子验证完成
- [ ] （可选）可视化材料准备

---

## 🎯 最终目标

### 论文实验部分完成度目标（今天结束）:

- **Baseline对比**: 100% ✅
  - BPR, LightGCN已有
  - Ours-Full已有

- **消融实验**: 100% ✅（今天完成）
  - w/o Contrast
  - w/o Mask
  - KG-only
  - CF-only

- **超参数**: 80% ✅（可选）
  - 已尝试第一轮，结果记录
  - 不强制需要更好的结果

- **多种子验证**: 20% ⏸️
  - 建议后续补充
  - 不影响今天的核心目标

### 明天（2026-01-14）可以开始：
- 论文方法论撰写
- 实验结果可视化
- 相关工作调研

---

**Created**: 2026-01-13 凌晨
**Status**: Ready for execution
**Priority**: 消融实验 > tune1决策 > 多种子验证
